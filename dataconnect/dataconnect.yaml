specVersion: "v1beta"
serviceId: "typescript"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "typescript-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
